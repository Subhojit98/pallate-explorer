import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    pallate: []
}

const pallateSlice = createSlice({
    name: "pllate",
    initialState,
    reducers: {

    }
})

// export { } = pallateSlice.actions
export default pallateSlice.reducer